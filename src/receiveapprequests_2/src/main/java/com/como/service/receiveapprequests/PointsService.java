package com.como.service.receiveapprequests;

import java.util.List;

import org.json.simple.JSONObject;
import org.springframework.stereotype.Service;

@Service
public class PointsService {
    // private JSONObject getItemByID(String item_id) {
    //     for (int i = 0; i < items.size(); i++) {
    //         JSONObject obj = items.get(i);
    //         if (obj.get("VoucherID").toString().equals(item_id)) {
    //             return obj;
    //         }
    //     }
    //     return null;
    // }

    // private int calculateTotalPtsCost(List<JSONObject> itemsToRedeem) {
    //     int total = 0;
    //     for (JSONObject item : itemsToRedeem) {
    //         double costDecimal = Double.parseDouble(item.get("CostValue").toString());
    //         int cost = (int) costDecimal;
    //         total += cost;
    //     }
    //     return total;
    // }
}
